# Runtime dependencies. Không có gì bắt buộc — project chạy được với Python
# chuẩn. Các thư viện dưới đây chỉ cần nếu bật các tính năng tương ứng.

# Claude API — cần để analyze_memory / generate_historical_entry có LLM thật.
# Nếu không cài, engine sẽ fallback về stub an toàn.
anthropic>=0.40.0

# Schema validation cho CLI submit (tuỳ chọn — submit.py đã có validator nội bộ).
jsonschema>=4.0.0

# RAG embedder: Voyage AI (Anthropic's recommended partner) cho production.
# Set VOYAGE_API_KEY để dùng. Hoặc set HUMANARCHIVE_USE_SENTENCE_TRANSFORMERS
# để dùng local sentence-transformers (nặng hơn, cần GPU cho tốc độ tốt).
# Nếu cả hai đều không có → fallback HashEmbedder (zero-dep, keyword match).
# voyageai>=0.2.0
# sentence-transformers>=2.5.0

# Federation: ký/verify bundle bằng ed25519. Chỉ cần nếu dùng --sign-key.
# cryptography>=42.0.0
